# sample_config.yml
general:
  run_id: "local-test"
  log_level: "INFO"

trading:
  symbol: "AAPL"
  timeframe: "1h"
  max_positions: 3

secrets:
  api_key: "${{ secrets.API_KEY }}"  # placeholder for GitHub Actions secret usage